---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  class={`flex justify-center items-center gap-10 w-full md:w-[720px] mx-auto md:p-0 px-10 ${className}`}
>
  <slot />
</section>
